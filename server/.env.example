PORT=5000
MONGO_URI=mongodb+srv://<db_username>:<db_password>@your-cluster.mongodb.net/studyos
JWT_SECRET=<generate_with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))">
NODE_ENV=production
CORS_ORIGINS=https://yourdomain.com
